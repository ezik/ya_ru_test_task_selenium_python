To install dependencies use:
- requirements.txt

Allure report can be generated with:
- Precondition: tests were ran at least once
- Allure command line installed:
brew install allure
- allure serve <dir>

Run with browser cli option:
pytest --browser=<browser_name>